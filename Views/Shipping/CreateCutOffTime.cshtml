@model CutOffTimeModel

@{
    Layout = "_AdminLayout";

    //page title
    ViewBag.Title = T("Nop.Plugins.Shipping.Manager.CutOffTimes.AddNew").Text;
    //active menu item (system name)
    NopHtml.SetActiveMenuItemSystemName("Nop.Plugins.Shipping.Manager.DatesAndRanges");
}

<form asp-controller="ShippingOperations" asp-action="CreateCutOffTime" method="post">
    <div class="content-header clearfix">
        <h1 class="float-left">
            @T("Plugins.Shipping.Manager.CutOffTimes.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="DatesAndRanges">@T("Plugins.Shipping.Manager.CutOffTimes.BackToList")</a>
            </small>
        </h1>
        <div class="float-right">
            <button type="submit" name="save" class="btn btn-primary">
                <i class="far fa-save"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn btn-primary">
                <i class="far fa-save"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = ShippingManagerWidgetZones.CutOffTimeDetailsButtons })
        </div>
    </div>
    <div class="content">
        @await Component.InvokeAsync("VendorScopeConfiguration")
        @await Html.PartialAsync("~/Plugins/SSI.Shipping.Manager/Views/Shipping/_CreateOrUpdateCutOffTime.cshtml")
    </div>
</form>
